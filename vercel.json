{
  "installCommand": "npm --prefix frontend ci && uv pip install --python .vercel/python/.venv/bin/python -r requirements.txt",
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/out",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
