{
  "installCommand": "npm --prefix frontend ci && python -m pip install -r requirements.txt",
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/out",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
